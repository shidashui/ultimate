# Tasks: Strip Markdown Before TTS

- [x] Add `_strip_markdown()` static method to `EdgeTTS` class
- [x] Call `_strip_markdown(text)` before passing to edge_tts in `synthesize()`
- [x] Run voice tests to confirm no regression
